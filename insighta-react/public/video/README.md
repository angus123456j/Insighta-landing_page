## Video folder

Drop your video files here (e.g. `hero.mp4`).

They will be available at runtime via:

- `/video/hero.mp4`

Example usage in React:

```jsx
<video autoPlay muted loop playsInline>
  <source src="/video/hero.mp4" type="video/mp4" />
</video>
```

